function checkTitleNull () {
    popupContentCheckTitleNode.classList.toggle(POPUP_OPENED_CLASSNAME);
    bodyNode.classList.toggle(BODY_FIXED_CLASSNAME);
};

function checkDescriptionNull () {
    popupContentCheckDescriptionNode.classList.toggle(POPUP_OPENED_CLASSNAME);
    bodyNode.classList.toggle(BODY_FIXED_CLASSNAME);
};

function checkTitleMore () {
    popupContentMoreTitleNode.classList.toggle(POPUP_OPENED_CLASSNAME);
    bodyNode.classList.toggle(BODY_FIXED_CLASSNAME);
};


function checkDescriptionMore () {
    popupContentMoreDescriptionNode.classList.toggle(POPUP_OPENED_CLASSNAME);
    bodyNode.classList.toggle(BODY_FIXED_CLASSNAME);
};

function removeAll() {
    removeAll.classList
    
}


